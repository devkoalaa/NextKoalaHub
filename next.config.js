/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['raw.githubusercontent.com', 'quatrorodas.abril.com.br'],
    },
}

module.exports = nextConfig
